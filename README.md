Steps to use ChadGPT in local:
- download the pack
- extract the pack
- install python
- install libraries in terminal: 'pip install asyncio websockets selenium webdriver-manager'
- open the python file
- open the html file
